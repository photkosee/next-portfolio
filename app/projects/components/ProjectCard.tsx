interface ProjectCardProps {
  name: string;
  desc: string;
  stacks: string[];
}

const ProjectCard = () => {
  return <div className="w-full rounded-3xl border"></div>;
};

export default ProjectCard;
